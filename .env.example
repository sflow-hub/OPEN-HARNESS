# Optional bootstrap credentials for the local control service.
# Settings stores replacements in .open-harness/secrets.json with mode 0600.
XAI_API_KEY=
OPENROUTER_API_KEY=
# Ollama, LM Studio, or another chat-completions-compatible server:
MODEL_BASE_URL=
MODEL_NAME=
MODEL_API_KEY=

# Optional server-side credentials. Or enter a session-only key in the UI.
XAI_API_KEY=
OPENROUTER_API_KEY=
# Ollama, LM Studio, or another chat-completions-compatible server:
MODEL_BASE_URL=
MODEL_NAME=
MODEL_API_KEY=

# npm installs fail rather than warn when the Node version does not satisfy
# "engines" in package.json. The coordinator imports node:sqlite, which does not
# exist before Node 22.13, so an unsatisfied engine is never survivable here.
engine-strict=true
